from setuptools import setup, find_packages

setup(
    name='FastXlsToCsv',
    version='1.0.0',
    description='A fast way to convert .xls and .xlsx to csv with vba',
    author='William Norland',
    packages=find_packages(),
    install_requires=[],  # Add any dependencies here
)